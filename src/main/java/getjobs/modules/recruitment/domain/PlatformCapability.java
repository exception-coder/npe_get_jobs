package getjobs.modules.recruitment.domain;

/** Capabilities exposed by a recruitment platform plugin. */
public enum PlatformCapability {
    SESSION,
    DISCOVER,
    FILTER,
    MATCH,
    CONTACT,
    APPLICATION_TRACKING
}
